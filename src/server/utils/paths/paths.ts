import { type Paths } from "./types";

export const paths: Paths = {
  root: "/",
  user: "/user",
  login: "/login",
  cars: "/cars",
  favorites: "/favorites",
  add: "/add",
  remove: "/remove",
};
